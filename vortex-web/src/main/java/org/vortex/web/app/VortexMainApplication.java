package org.vortex.web.app;

import io.dropwizard.Application;
import io.dropwizard.setup.Environment;
import org.vortex.web.config.VortexConfiguration;

public class VortexMainApplication extends Application<VortexConfiguration> {
    @Override
    public void run(VortexConfiguration configuration, Environment environment) throws Exception {

    }
}
